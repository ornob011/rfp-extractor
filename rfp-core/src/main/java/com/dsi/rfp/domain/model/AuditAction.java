package com.dsi.rfp.domain.model;

public enum AuditAction {
    LOGIN,
    LOGOUT,
    SUBMIT_DOCUMENT,
    VIEW_RESULT,
    DOWNLOAD_ARTIFACT,
    RELOAD_RULE_PACK,
    VIEW_ADMIN,
    ACCESS_DENIED
}
